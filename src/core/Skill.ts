export interface Skill {
  name: string;      // from the first # heading in the file, or the filename
  fileName: string;  // e.g. "property-portal-navigation.md"
  content: string;   // full markdown content
}
